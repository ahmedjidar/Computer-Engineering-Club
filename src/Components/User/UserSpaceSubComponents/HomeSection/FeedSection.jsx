import React from "react";
import FullPost from "../BlogPostElements/FullPost";

const FeedSection = () => {

    return(
        // list of feed posts to handle later / for now a demo post is used :)
        <div>
            <FullPost/>
        </div>
    )
}

export default FeedSection;